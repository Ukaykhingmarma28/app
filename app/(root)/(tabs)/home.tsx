import { Text, View } from "react-native";

const Home = () => {
  return (
    <View className={"flex-1 justify-center items-center"}>
      <Text className={"text-red-400 text-2xl"}>Home</Text>
    </View>
  );
};

export default Home;
